[from-domophone]
exten => 1001,1,NoOp(Intercom incoming)
 same => n,Ringing()
 same => n,Stasis(intercom)
 same => n,Hangup()

exten => busy,1,NoOp(Busy - user rejected)
 same => n,Playback(ru/is-in-use)
 same => n,Hangup()

exten => noanswer,1,NoOp(No answer - timeout)
 same => n,Playback(ru/number-not-answering)
 same => n,Hangup()
